#include<bits/stdc++.h>
#include "GroundTruth.h"

using namespace std;
int main(){
    uint8_t key[10];
    uint64_t tmp;
    rep2(i, 0, 10){
        fin.read((char*) &tmp, sizeof(char)*8);
        cout << (uint64_t)tmp << endl;
        
        memset(key, 0, sizeof(key));
    }
}